# Read the Docs configuration for the open BMC/firmware documentation.
# https://docs.readthedocs.io/en/stable/config-file/v2.html
version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"

sphinx:
  configuration: source/conf.py
  fail_on_warning: true

python:
  install:
    - requirements: requirements.txt

formats:
  - pdf
  - htmlzip
